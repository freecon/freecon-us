dependencies:
  post:
    - npm install -g divshot-cli gulp-cli
test:
  override:
    - gulp 
deployment:
  divshot:
    branch: master
    commands:
      - mkdir -p ~/.divshot/config/
      - echo "$DIVSHOT_TOKEN" > ~/.divshot/config/user.json
      - mkdir pub
      - cp -Rvp index.html sutro.jpg lib/ pub/
      - divshot push
